package org.tribetalk.ui.theme

import androidx.compose.ui.graphics.Color

// =============================================================================
// TRIBE TALK: HIGH-CONTRAST GREEN, WHITE, AND BLACK DESIGN SYSTEM
// =============================================================================

// 1. Vibrant Electric Emerald & Mint Green Palette
val EmeraldGreen = Color(0xFF00E676)       // Vibrant Electric Neon Green (Primary Accents & Badges)
val EmeraldPrimary = Color(0xFF10B981)     // Rich Emerald Green (Pills, Buttons, Highlights)
val EmeraldDark = Color(0xFF059669)        // Deep Forest Emerald (Borders, Active States)
val EmeraldContainerDark = Color(0xFF064E3B) // Dark Emerald Container
val EmeraldContainerLight = Color(0xFFD1FAE5) // Soft Mint Container
val EmeraldMint = Color(0xFFA7F3D0)        // Bright Mint Green (High-Contrast Text on Dark)
val EmeraldGlow = Color(0x4000E676)        // Electric Green Glow Shadow

// 2. Pitch Black & Obsidian Dark Surfaces
val PureBlack = Color(0xFF000000)          // Pitch Black (True OLED Background)
val DarkSurface = Color(0xFF0A0A0C)        // Deep Obsidian (App Bars & Panels)
val DarkCard = Color(0xFF141416)           // High-Contrast Dark Card
val DarkCardElevated = Color(0xFF1C1C1F)   // Elevated Obsidian Surface
val DarkBorder = Color(0xFF27272A)         // Charcoal Dark Border (Zinc 800)
val DarkBorderGreen = Color(0xFF059669)    // Crisp Emerald Accent Border

// 3. Crisp Pure White & High-Legibility Light Elements
val PureWhite = Color(0xFFFFFFFF)          // Pure White (Headings, Primary Text, Contrast Badges)
val OffWhite = Color(0xFFF4F4F5)           // Soft White Surface
val WhiteSecondary = Color(0xFFA1A1AA)     // Zinc 400 (Secondary Descriptions)
val WhiteMuted = Color(0xFF71717A)         // Zinc 500 (Muted Hints & Timestamps)
val CrispBorderLight = Color(0xFFE4E4E7)   // Light Mode Crisp Border

// 4. Status Indicators (Strictly harmonious with Green / White / Black)
val StatusListening = Color(0xFF00E676)    // Electric Green Pulse for Recording
val StatusTranscribing = Color(0xFFFBBF24) // Amber Glow
val StatusTranslating = Color(0xFF38BDF8)  // Sky Cyan
val StatusSynthesizing = Color(0xFFA78BFA) // Mint Violet
val StatusReady = Color(0xFF00E676)        // Electric Green Ready Dot
